// Array

// const nome = 'João'

// const nomes = ['João', 'Marcos', 'Marcus', 'John', 'Cleber']

// console.log(nome)

// console.log(nomes[4])

function Title(css, textContent) {
  console.log(css)

  return `
    <h1 style="${css[0]}">
      ${textContent}
    </h1>`
}